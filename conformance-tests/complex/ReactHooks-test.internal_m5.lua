it(
	("warns when more hooks (%s) are used during update than mount"):format((function()
		error("not implemented") --[[ ROBLOX TODO: Lua doesn't support 'Identifier' as a standalone type ]] --[[ hookNameA ]]
		return hookNameB
	end)()),
	function() end
)
